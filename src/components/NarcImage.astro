---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
}

const { src, alt } = Astro.props;
---

<style>
  .narc-image {
    max-width: min(100%, 450px);
    height: 100%;
  }
</style>

<Image
  class={"narc-image"}
  src={src}
  alt={alt}
  width={450}
  widths={[375, 450]}
  sizes={`(max-width: 786px) 375px, 450px`}
/>
